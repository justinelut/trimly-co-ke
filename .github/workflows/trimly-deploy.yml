# =============================================================================
# Trimly — build & deploy to K3s
#
# On every push to main:
#   1. Build the Docker image
#   2. Push to ghcr.io/<owner>/trimly-co-ke:<sha> and :latest
#   3. SSH into the K3s server, kubectl apply manifests, set the new image
#      tag on the Deployment, and wait for the rollout
#
# Prisma migrations are NOT run here (deliberate — see AGENTS.md §2 and the
# Trimly build brief §2). Run them by hand after a schema change with:
#   kubectl delete job trimly-co-ke-migrate -n trimly-co-ke --ignore-not-found
#   kubectl apply -f k8s/migrate-job.yaml
#   kubectl logs -f job/trimly-co-ke-migrate -n trimly-co-ke
# =============================================================================
name: Build & Deploy

on:
  push:
    branches: [main]
    paths:
      # Only trigger Trimly deploy when files relevant to it change.
      # Cal-only paths (cron-*.yml, app-store internals, etc.) won't redeploy.
      - 'apps/web/**'
      - 'packages/prisma/schema.prisma'
      - 'packages/**'
      - 'k8s/**'
      - 'Dockerfile'
      - '.dockerignore'
      - 'package.json'
      - 'yarn.lock'
      - '.github/workflows/trimly-deploy.yml'
  workflow_dispatch: {}

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false        # never cancel a running deploy

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/trimly-co-ke
  K3S_NAMESPACE: trimly-co-ke
  K3S_DEPLOYMENT: trimly-co-ke-deployment
  K3S_CONTAINER: web

jobs:
  # ---------------------------------------------------------------------------
  # 1. Build & push to GHCR
  # ---------------------------------------------------------------------------
  build:
    name: Build & push image
    # Native ARM64 runner — builds at full speed without QEMU emulation.
    # K3s nodes are ARM, so building on an ARM runner produces the right
    # arch directly. Requires a paid GitHub plan for private repos.
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read
      packages: write              # required to push to GHCR
    outputs:
      image-tag: ${{ steps.meta.outputs.version }}
      image-ref: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # GHA runners start with ~14GB free disk. Cal's multi-stage Dockerfile
      # (three full node_modules snapshots, Prisma artifacts, embed bundles,
      # .next caches) blows past that. This action prunes preinstalled tools
      # we don't need and gets us back to ~50GB free. Adds ~60s to the run.
      - name: Free disk space on runner
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false        # keep Node toolchain
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: false     # we need buildx layer cache
          swap-storage: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile               # cal.diy's existing multi-stage Dockerfile
          push: true
          platforms: linux/arm64           # K3s nodes are ARM64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Build args. Real DATABASE_URL/NEXTAUTH_SECRET/etc. are injected at
          # runtime via the Kubernetes Secret — these are just placeholders so
          # Prisma client generation + Next.js build can complete.
          build-args: |
            BUILD_STANDALONE=true
            NEXT_PUBLIC_WEBAPP_URL=https://trimly.co.ke
            NEXT_PUBLIC_API_V2_URL=https://trimly.co.ke/api
            DATABASE_URL=postgres://build:build@localhost:5432/build
            NEXTAUTH_SECRET=build-time-placeholder-replaced-at-runtime
            CALENDSO_ENCRYPTION_KEY=build_time_encryption_key_32chr
            CALCOM_TELEMETRY_DISABLED=1
            MAX_OLD_SPACE_SIZE=6144
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false        # smaller manifest, faster pull on K3s

  # ---------------------------------------------------------------------------
  # 2. Deploy to K3s via SSH (appleboy actions — handle keepalive internally)
  # ---------------------------------------------------------------------------
  deploy:
    name: Deploy to K3s
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read              # mints GITHUB_TOKEN with GHCR pull rights
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create target directory on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo mkdir -p /opt/trimly-co-ke/k8s
            sudo chown -R ubuntu:ubuntu /opt/trimly-co-ke

      - name: Copy k8s manifests to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "k8s/namespace.yaml,k8s/configmap.yaml,k8s/postgres.yaml,k8s/minio.yaml,k8s/deploy.yaml,k8s/cronjobs.yaml,k8s/ingress.yaml,k8s/middleware-redirect.yaml"
          target: "/opt/trimly-co-ke/k8s/"
          strip_components: 1

      - name: Apply manifests and roll deployment
        uses: appleboy/ssh-action@v1.0.3
        env:
          IMAGE_TAG: ${{ needs.build.outputs.image-tag }}
          # ---- App + DB secret keys (sourced from GitHub repo Secrets) ----
          POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
          CALENDSO_ENCRYPTION_KEY: ${{ secrets.CALENDSO_ENCRYPTION_KEY }}
          PAYSTACK_SECRET_KEY: ${{ secrets.PAYSTACK_SECRET_KEY }}
          PAYSTACK_WEBHOOK_SECRET: ${{ secrets.PAYSTACK_WEBHOOK_SECRET }}
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          MINIO_ROOT_USER: ${{ secrets.MINIO_ROOT_USER }}
          MINIO_ROOT_PASSWORD: ${{ secrets.MINIO_ROOT_PASSWORD }}
          STORAGE_ACCESS_KEY: ${{ secrets.STORAGE_ACCESS_KEY }}
          STORAGE_SECRET_KEY: ${{ secrets.STORAGE_SECRET_KEY }}
          AT_USERNAME: ${{ secrets.AT_USERNAME }}
          AT_API_KEY: ${{ secrets.AT_API_KEY }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          POSTHOG_KEY: ${{ secrets.POSTHOG_KEY }}
          POSTHOG_HOST: ${{ secrets.POSTHOG_HOST }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          # ---- GHCR pull credentials (auto-issued, zero setup) ----
          # GitHub mints GITHUB_TOKEN with packages:read scope on each run.
          # github.actor is whoever triggered the run.
          GHCR_USERNAME: ${{ github.actor }}
          GHCR_PULL_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: IMAGE_TAG,POSTGRES_PASSWORD,DATABASE_URL,NEXTAUTH_SECRET,CALENDSO_ENCRYPTION_KEY,PAYSTACK_SECRET_KEY,PAYSTACK_WEBHOOK_SECRET,RESEND_API_KEY,MINIO_ROOT_USER,MINIO_ROOT_PASSWORD,STORAGE_ACCESS_KEY,STORAGE_SECRET_KEY,AT_USERNAME,AT_API_KEY,GOOGLE_CLIENT_ID,GOOGLE_CLIENT_SECRET,POSTHOG_KEY,POSTHOG_HOST,SENTRY_DSN,CRON_SECRET,GHCR_USERNAME,GHCR_PULL_TOKEN
          command_timeout: 12m
          script: |
            set -e
            cd /opt/trimly-co-ke/k8s

            NS=trimly-co-ke
            DEP=trimly-co-ke-deployment
            LABEL=app=trimly-co-ke

            # 1. Ensure namespace exists before secrets are created.
            sudo kubectl apply -f namespace.yaml

            # 2. Sync the app/db Secret from GitHub Actions secrets.
            #    --dry-run=client | apply makes this idempotent: creates on
            #    first run, updates on every subsequent run. Values never
            #    appear on stdout because kubectl reads them as flag args.
            sudo kubectl create secret generic trimly-co-ke-secret -n "$NS" \
              --dry-run=client -o yaml \
              --from-literal=POSTGRES_PASSWORD="${POSTGRES_PASSWORD}" \
              --from-literal=DATABASE_URL="${DATABASE_URL}" \
              --from-literal=NEXTAUTH_SECRET="${NEXTAUTH_SECRET}" \
              --from-literal=CALENDSO_ENCRYPTION_KEY="${CALENDSO_ENCRYPTION_KEY}" \
              --from-literal=PAYSTACK_SECRET_KEY="${PAYSTACK_SECRET_KEY}" \
              --from-literal=PAYSTACK_WEBHOOK_SECRET="${PAYSTACK_WEBHOOK_SECRET}" \
              --from-literal=RESEND_API_KEY="${RESEND_API_KEY}" \
              --from-literal=MINIO_ROOT_USER="${MINIO_ROOT_USER:-trimly_admin}" \
              --from-literal=MINIO_ROOT_PASSWORD="${MINIO_ROOT_PASSWORD}" \
              --from-literal=STORAGE_ACCESS_KEY="${STORAGE_ACCESS_KEY:-${MINIO_ROOT_USER:-trimly_admin}}" \
              --from-literal=STORAGE_SECRET_KEY="${STORAGE_SECRET_KEY:-${MINIO_ROOT_PASSWORD}}" \
              --from-literal=AT_USERNAME="${AT_USERNAME:-placeholder}" \
              --from-literal=AT_API_KEY="${AT_API_KEY:-placeholder}" \
              --from-literal=GOOGLE_CLIENT_ID="${GOOGLE_CLIENT_ID:-placeholder.apps.googleusercontent.com}" \
              --from-literal=GOOGLE_CLIENT_SECRET="${GOOGLE_CLIENT_SECRET:-placeholder}" \
              --from-literal=POSTHOG_KEY="${POSTHOG_KEY:-placeholder}" \
              --from-literal=POSTHOG_HOST="${POSTHOG_HOST:-https://eu.posthog.com}" \
              --from-literal=SENTRY_DSN="${SENTRY_DSN:-}" \
              --from-literal=CRON_SECRET="${CRON_SECRET}" \
              | sudo kubectl apply -f -
            echo "==> trimly-co-ke-secret synced"

            # 3. Sync the GHCR image-pull Secret using the auto-issued
            #    GITHUB_TOKEN (no manual PAT needed). permissions:packages:read
            #    on this job lets the token pull from GHCR. The token is
            #    short-lived, but containerd caches the image after the first
            #    pull triggered by `kubectl set image` below — subsequent pod
            #    restarts use the cached image, no re-pull, no auth needed.
            sudo kubectl create secret docker-registry ghcr-pull-secret -n "$NS" \
              --dry-run=client -o yaml \
              --docker-server=ghcr.io \
              --docker-username="${GHCR_USERNAME}" \
              --docker-password="${GHCR_PULL_TOKEN}" \
              --docker-email="ops@trimly.co.ke" \
              | sudo kubectl apply -f -
            echo "==> ghcr-pull-secret synced (via GITHUB_TOKEN)"

            # 4. Apply the rest of the manifests (namespace already done above).
            for f in configmap.yaml postgres.yaml minio.yaml deploy.yaml cronjobs.yaml ingress.yaml middleware-redirect.yaml; do
              if [ -f "$f" ]; then
                echo "==> kubectl apply -f $f"
                sudo kubectl apply -f "$f"
              fi
            done

            sudo kubectl set image deployment/$DEP \
              web=ghcr.io/justinelut/trimly-co-ke:${IMAGE_TAG} \
              -n $NS

            # Rolling out with a background rollout-status + foreground
            # poll loop. The poll emits pod state every 15s so SSH stays
            # warm AND we see what's happening live. On failure we dump
            # describe + logs (current + previous) — diagnose without
            # needing to SSH in.
            sudo kubectl rollout status "deployment/$DEP" -n "$NS" --timeout=8m &
            ROLLOUT_PID=$!

            while kill -0 $ROLLOUT_PID 2>/dev/null; do
              sleep 15
              echo "---- $(date -u +%H:%M:%S) pod state ----"
              sudo kubectl -n "$NS" get pods -l $LABEL -o wide --no-headers 2>/dev/null || true
              POD=$(sudo kubectl -n "$NS" get pods -l $LABEL \
                --sort-by=.metadata.creationTimestamp \
                -o jsonpath='{.items[-1:].metadata.name}' 2>/dev/null || echo "")
              if [ -n "$POD" ]; then
                sudo kubectl -n "$NS" describe pod "$POD" 2>/dev/null \
                  | awk '/^Events:/,0' | head -n 12 || true
              fi
            done

            wait $ROLLOUT_PID
            RC=$?

            if [ $RC -ne 0 ]; then
              echo ""
              echo "========================================"
              echo " ROLLOUT FAILED — DIAGNOSTIC DUMP"
              echo "========================================"
              echo ""
              echo "==> All pods in namespace:"
              sudo kubectl -n "$NS" get pods -o wide 2>&1 || true
              echo ""
              echo "==> Recent events (sorted, last 30):"
              sudo kubectl -n "$NS" get events --sort-by=.lastTimestamp 2>&1 | tail -30 || true
              echo ""
              echo "==> describe pod (newest matching $LABEL):"
              sudo kubectl -n "$NS" describe pod -l $LABEL 2>&1 | tail -n 80 || true
              echo ""
              echo "==> current logs (tail 200):"
              sudo kubectl -n "$NS" logs deployment/$DEP --tail=200 --all-containers=true 2>&1 || true
              echo ""
              echo "==> previous logs (if crashed):"
              sudo kubectl -n "$NS" logs deployment/$DEP --tail=200 --all-containers=true --previous 2>&1 || true
              exit $RC
            fi

            echo "✅ Deployed ghcr.io/justinelut/trimly-co-ke:${IMAGE_TAG}"

      - name: Summary
        if: always()
        run: |
          echo "### Deploy summary" >> $GITHUB_STEP_SUMMARY
          echo "- Image: \`ghcr.io/justinelut/trimly-co-ke:${{ needs.build.outputs.image-tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Namespace: \`trimly-co-ke\`" >> $GITHUB_STEP_SUMMARY
          echo "- Deployment: \`trimly-co-ke-deployment\`" >> $GITHUB_STEP_SUMMARY
          echo "- URL: https://trimly.co.ke" >> $GITHUB_STEP_SUMMARY
